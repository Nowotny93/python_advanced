from python_modules.math_operations1.operation_helper import *

operation_mapper = {
    '+': apply_sum,
    '-': apply_subtract,
    '^': apply_power,
    '*': apply_multiply,
    '/': apply_divide
}